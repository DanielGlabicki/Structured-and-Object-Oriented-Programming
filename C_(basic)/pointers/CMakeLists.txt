cmake_minimum_required(VERSION 3.13)

set(PROJECT_ID pointers)

set(SOURCE_FILES
        # TODO: Uzupełnij - podaj ścieżki do "podstawowych" plików źródłowych.
        src/ptrlib.c)

set(SOURCES_FILES_TESTS
        # TODO: Uzupełnij - podaj ścieżki do plików źródłowych testów jednostkowych.
        test/test_ptrlib.cpp)

include(../SetCommonConfig.cmake)
